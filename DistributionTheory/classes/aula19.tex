\documentclass[../distribution_theory_notes.tex]{subfiles}
\begin{document}
\section{Aula 19 - 01 de Novembro, 2024}
\subsection{Motivações}
\begin{itemize}
	\item Localizações
\end{itemize}
\subsection{Localizações}
Nas aulas passadas, introduzimos e começamos a provar o resultado que determina que uma distribuição fica completamente determinada por suas localizações:
\begin{theorem*}
	Se \(\{U_{\lambda }\}_{\lambda \in L}\) é uma família de abertos de modo que, a cada \(\lambda \in L\) está associada uma \(u_{\lambda }\in \mathcal{D}'(U_{\lambda })\) tal que \(U_{\lambda }\cap U_{\mu }\neq \emptyset \) implica nas distribuições \(u_{\lambda }=u_{\mu }\) em \(U_{\lambda }\cap U_{\mu }\), então existe uma única \(u\in \mathcal{D}'(U)\), onde \(U\coloneqq \bigcup_{\lambda \in L}^{}U_{\lambda }\), tal que
	\[
		u|_{U_{\lambda }} = u_{\lambda }, \quad \forall \lambda \in L.
	\]
\end{theorem*}
\begin{proof*}
	Dado \(K\subseteq U\), tomando uma subcoleção \(K\subseteq U_{\lambda_1}\cup \dotsc \cup U_{\lambda_{m}}\) e \(\psi_1, \dotsc , \psi_{m}\) uma partição da unidade associada, para \(\varphi \in \mathcal{C}_{c}^{\infty}(K)\) pomos
	\[
		\langle u, \varphi  \rangle \coloneqq \sum\limits_{j=1}^{m}\langle u_{\lambda_{j}}, \underbrace{\psi_{j}\varphi }_{\in \mathcal{C}_{c}^{\infty}(U_{\lambda_{j}})} \rangle.
	\]
	Nas condições acima, vimos que
\end{proof*}
\end{document}
